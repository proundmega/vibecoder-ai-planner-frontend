import { KanbanCardModalComponent } from './kanban-card-modal.component';

describe('KanbanCardModalComponent', () => {
  it('should be defined', () => {
    expect(KanbanCardModalComponent).toBeDefined();
  });
});
